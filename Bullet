//Create a Bullet prefab add a script to it.Add this
public float bulletspeed;
    
    void Update()
    {
        Vector3 bulletmove = new Vector3(0f,-1f,0f)*bulletspeed*Time.deltaTime;
        transform.Translate(bulletmove,Space.Self);
    }
